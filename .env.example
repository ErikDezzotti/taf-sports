# ============================================================================
# TAF SPORTS - ENVIRONMENT CONFIGURATION (EXEMPLO)
# ============================================================================
# COPIE este arquivo para .env e ajuste os valores
# ============================================================================

# ENVIRONMENT (NÃO MEXE - detectado automaticamente pelo hostname)
# development = localhost | production = servidor
ENVIRONMENT=development

# DATABASE LOCAL (desenvolvimento)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=taf-database

# SECURITY KEYS
ENCRYPTION_KEY=320883c684fa69a481c041d0a32e6590

# EMAIL (configure em produção)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=contato@tafsports.com.br
SMTP_PASS=
SMTP_FROM_EMAIL=contato@tafsports.com.br
SMTP_FROM_NAME=TAF Sports

# LOGGING
LOG_THRESHOLD=1
